{
  "name": "autofill-job-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Firefox Greenhouse autofill extension with localhost GitHub Models proxy.",
  "scripts": {
    "start:proxy": "node proxy/server.js",
    "dev:proxy": "node --watch proxy/server.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "pdf-parse": "^1.1.1"
  }
}
